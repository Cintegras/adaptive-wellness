{
  "name": "adaptive-wellness",
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "nohoist": [
    "**/react-native",
    "**/expo-*",
    "**/metro-*"
  ],
  "scripts": {
    "start:formcoach-mobile": "pnpm --filter formcoach-mobile start",
    "build": "nx run-many --target=build",
    "test": "nx run-many --target=test"
  },
  "devDependencies": {
    "nx": "^16.0.0",
    "pnpm": "^8.0.0"
  },
  "dependencies": {
    "expo": "^53.0.9"
  }
}
